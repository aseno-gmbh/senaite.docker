apiVersion: v1
kind: Service
metadata:
  name:  senaite
  namespace: {{ .Release.Namespace }}
  labels:
    app: senaite
spec:
  selector:
    app: senaite
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.deployment.containerPort }}